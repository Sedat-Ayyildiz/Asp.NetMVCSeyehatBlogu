@using TravelBlog.Models.Siniflar
@model List<Blog>

<div class="box1-left">
    @foreach (var x in Model)
    {
        <div class="bot-img">
            <a href="/Blog/BlogDetay/@x.ID"><img src="@x.BLOGIMAGE" alt="" /></a>
        </div>
        <div class="bot-desc1">
            <p>@x.ACIKLAMA.Substring(0,75) ...</p>
        </div>
        <div class="clear"></div>
    }
</div>
